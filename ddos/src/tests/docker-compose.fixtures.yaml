version: "3.9"
name: smvend-int-tests
services:
  dnb-main-pg-fixtures:
    image: "dnb-main-pg-fixtures:latest"
    container_name: "dnb-main-pg-fixtures"
    entrypoint: ["npm", "run", "fixture:up", "--", "-e", "dynamic-db"]
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dnb-main-pg
      POSTGRES_HOST: satan-for-test
